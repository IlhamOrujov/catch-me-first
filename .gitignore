# secrets — real API keys must never be committed
src/keys.local.js
KEYS-BACKUP*.txt

# tooling
node_modules
.vercel
.netlify
.DS_Store
*.log

# Developer Mode local notes/exports (may hold private notes; read from local repo)
DEV-NOTES.md
dev-exports/
